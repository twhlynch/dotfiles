<important>
Avoid accessing external directories:
Instead of using /tmp, just use the cwd.
Instead of searching external library code, look for usage examples in the cwd.

When the project is a large compiled project, do not start builds to verify changes unless prompted to do so.

Never run git commands that modify files or staged state.
</important>
